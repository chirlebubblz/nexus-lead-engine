import { processEnrichment } from './src/lib/enrichment.js';

// We need to compile the TS first or use ts-node, simplistic test instead:
console.log("TS needs compiling to test module directly, running simpler test:");
