import { createClient } from "@supabase/supabase-js"; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { console.log("Fetching a pending lead..."); const { data, error } = await supabase.from("leads").select("*").eq("status", "pending").limit(1); if (error || !data || data.length === 0) { console.log("No pending leads found in DB."); return; } const lead = data[0]; console.log(`Triggering enrichment for: ${lead.business_name} (ID: ${lead.id})`); try { const res = await fetch("http://localhost:3000/api/enrich", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ leadId: lead.id }) }); const text = await res.text(); console.log(`Response Status: ${res.status}`); console.log(`Response Body: ${text}`); } catch (err) { console.error("Fetch Error:", err); } } run();